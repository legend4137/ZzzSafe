--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu  # Downgraded for cloud compatibility
torchvision==0.15.2+cpu
opencv-python-headless==4.7.0.72  # Headless version for cloud
mediapipe==0.10.5  # Pinned to stable version
numpy==1.24.3  # Slightly older for better compatibility
scipy==1.10.1
streamlit==1.28.0
filelock==3.13.1  # Explicitly added torch dependency
typing-extensions==4.9.0  # Required by torch
